<Page
    x:Class="Daiso.App.Views.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:Daiso.App.Strings"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:vm="using:Daiso.App.ViewModels"
    xmlns:terminal="using:Daiso.App.Terminal">

    <ScrollViewer Padding="{StaticResource PagePadding}" VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="{StaticResource ContentMaxWidth}" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="16" Margin="0,0,0,20">

            <StackPanel Spacing="2">
                <TextBlock Text="{loc:Str Key=Terminal_Title}" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="{loc:Str Key=Terminal_ShellNote}" Style="{StaticResource PageSubtitleText}" />
            </StackPanel>

            <!-- 작업 폴더 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel Spacing="10">
                    <TextBlock Text="{loc:Str Key=Terminal_WorkFolder}" Style="{StaticResource SectionTitleText}" />

                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox
                            Grid.Column="0"
                            AutomationProperties.AutomationId="WorkingDirBox"
                            AutomationProperties.Name="{loc:Str Key=Terminal_FolderPath}"
                            FontFamily="Consolas"
                            Text="{x:Bind ViewModel.WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="{loc:Str Key=Terminal_FolderPath}" />

                        <muxc:DropDownButton
                            Grid.Column="1"
                            AutomationProperties.AutomationId="ProjectPickerButton">
                            <muxc:DropDownButton.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{loc:Str Key=Common_KnownProjects}" />
                                </StackPanel>
                            </muxc:DropDownButton.Content>
                            <muxc:DropDownButton.Flyout>
                                <MenuFlyout x:Name="ProjectFlyout" Opening="OnProjectFlyoutOpening" />
                            </muxc:DropDownButton.Flyout>
                        </muxc:DropDownButton>
                    </Grid>

                    <TextBlock Text="{loc:Str Key=Terminal_RecentFolders}" Style="{StaticResource MutedText}" />

                    <ListView
                        AutomationProperties.AutomationId="RecentFolderList"
                        ItemsSource="{x:Bind ViewModel.RecentFolders}"
                        SelectionMode="Single"
                        MaxHeight="150"
                        SelectionChanged="OnRecentSelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock
                                    Text="{x:Bind}"
                                    Style="{StaticResource MonoText}"
                                    ToolTipService.ToolTip="{x:Bind}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Border Style="{StaticResource NoticeBorder}" Visibility="{x:Bind ViewModel.HasNoRecentFolders, Mode=OneWay}">
                        <TextBlock Text="{loc:Str Key=Terminal_NoRecent}" Style="{StaticResource PageSubtitleText}" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- 도구 탭이 붙은 카드: 탭 띠가 카드 머리에 있고 그 아래 내용이 탭을 따른다. 순서는 ToolLook.DisplayOrder -->
            <Border Style="{StaticResource CardBorder}" Padding="0">
                <StackPanel>
                    <Border
                        Padding="12,8,12,0"
                        BorderThickness="0,0,0,1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <muxc:SelectorBar x:Name="ToolTabs" SelectionChanged="OnToolTabChanged" AutomationProperties.AutomationId="ToolTabs">
                            <muxc:SelectorBarItem Text="Codex" IsSelected="True" AutomationProperties.AutomationId="ToolTabCodex">
                                <muxc:SelectorBarItem.Icon>
                                    <FontIcon Glyph="&#xE756;" />
                                </muxc:SelectorBarItem.Icon>
                            </muxc:SelectorBarItem>
                            <muxc:SelectorBarItem Text="Claude" AutomationProperties.AutomationId="ToolTabClaude">
                                <muxc:SelectorBarItem.Icon>
                                    <FontIcon Glyph="&#xE756;" />
                                </muxc:SelectorBarItem.Icon>
                            </muxc:SelectorBarItem>
                            <muxc:SelectorBarItem Text="Gemini" AutomationProperties.AutomationId="ToolTabGemini">
                                <muxc:SelectorBarItem.Icon>
                                    <FontIcon Glyph="&#xE756;" />
                                </muxc:SelectorBarItem.Icon>
                            </muxc:SelectorBarItem>
                        </muxc:SelectorBar>
                    </Border>

                <StackPanel Spacing="14" Padding="20,16,20,20">
                    <!-- 인자 -->
                    <StackPanel Spacing="10">
                        <TextBlock Text="{loc:Str Key=Terminal_OptionArgs}" Style="{StaticResource SectionTitleText}" />
                        <TextBox
                            AutomationProperties.AutomationId="ArgumentsBox"
                            AutomationProperties.Name="{loc:Str Key=Terminal_OptionArgs}"
                            Text="{x:Bind ViewModel.SelectedTool.Arguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="{loc:Str Key=Terminal_ArgsPlaceholder}" />

                        <TextBlock Text="{x:Bind ViewModel.SelectedTool.PresetsTitle, Mode=OneWay}" Style="{StaticResource MutedText}" />
                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.SelectedTool.Presets, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <UniformGridLayout
                                    MinItemWidth="170"
                                    MinRowSpacing="8"
                                    MinColumnSpacing="8"
                                    ItemsStretch="None" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Button Content="{x:Bind}" Tag="{x:Bind}" Click="OnPresetClick" HorizontalAlignment="Stretch" />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- 실행: 설치돼 있으면 열기, 아니면 설치. 설치가 도는 동안은 잠긴다 -->
                    <StackPanel Spacing="10">
                        <TextBlock Text="{loc:Str Key=Terminal_Launch}" Style="{StaticResource SectionTitleText}" />

                        <StackPanel Spacing="6" HorizontalAlignment="Left" MinWidth="260">
                            <Button
                                HorizontalAlignment="Stretch"
                                Style="{ThemeResource AccentButtonStyle}"
                                AutomationProperties.AutomationId="LaunchButton"
                                Command="{x:Bind ViewModel.LaunchCommand}"
                                CommandParameter="{x:Bind ViewModel.SelectedTool, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.SelectedTool.CanPress, Mode=OneWay}">
                                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                    <FontIcon Glyph="{x:Bind ViewModel.SelectedTool.Glyph, Mode=OneWay}" FontSize="14" />
                                    <TextBlock Text="{x:Bind ViewModel.SelectedTool.ButtonText, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedTool.Hint, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.SelectedTool.HasHint, Mode=OneWay}"
                                Style="{StaticResource MutedText}"
                                TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="{loc:Str Key=Terminal_Preview}" Style="{StaticResource MutedText}" />
                            <TextBlock
                                AutomationProperties.AutomationId="LaunchPreview"
                                Text="{x:Bind ViewModel.SelectedTool.Preview, Mode=OneWay}"
                                Style="{StaticResource MonoText}"
                                TextTrimming="CharacterEllipsis"
                                ToolTipService.ToolTip="{x:Bind ViewModel.SelectedTool.Preview, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel Spacing="4" Visibility="{x:Bind ViewModel.HasLastCommand, Mode=OneWay}">
                            <TextBlock Text="{loc:Str Key=Terminal_LastCommand}" Style="{StaticResource MutedText}" />
                            <TextBlock
                                Text="{x:Bind ViewModel.LastCommand, Mode=OneWay}"
                                Style="{StaticResource MonoText}"
                                TextWrapping="Wrap"
                                IsTextSelectionEnabled="True" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                </StackPanel>
            </Border>

            <!-- 대화 방: 앱 안에서 CLI를 띄우고 채팅 블록으로 본다. 터미널↔채팅은 같은 자리를 토글로 바꾼다 (ARCHITECTURE §5.3) -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel Spacing="10">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{loc:Str Key=Terminal_EmbeddedExperiment}" Style="{StaticResource SectionTitleText}" />
                        <Button
                            Grid.Column="1"
                            Content="{loc:Str Key=Terminal_OpenHere}"
                            Style="{ThemeResource AccentButtonStyle}"
                            AutomationProperties.AutomationId="EmbeddedOpenButton"
                            Click="OnEmbeddedOpenClick" />
                    </Grid>
                    <TextBlock
                        x:Name="EmbeddedStatus"
                        Style="{StaticResource MutedText}"
                        TextWrapping="Wrap"
                        Visibility="Collapsed" />

                    <!-- 열린 방 탭. 여러 방을 열고 탭으로 오간다. 방은 RoomManager가 들고 있어 화면을 옮겨도 산다 -->
                    <ListView
                        x:Name="RoomTabs"
                        SelectionMode="Single"
                        SelectionChanged="OnRoomTabChanged"
                        AutomationProperties.AutomationId="RoomTabs"
                        Visibility="Collapsed"
                        ScrollViewer.HorizontalScrollMode="Auto"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollMode="Disabled">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="6" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="4" />
                                <Setter Property="MinWidth" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:ChatRoomViewModel">
                                <Grid ColumnSpacing="8" Padding="6,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="20" Height="20" CornerRadius="10" VerticalAlignment="Center" Background="{x:Bind AvatarBrush}">
                                        <TextBlock Text="{x:Bind Avatar}" Foreground="White" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="{x:Bind ShortTitle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="140" />
                                    <Button
                                        Grid.Column="2"
                                        Padding="4"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        AutomationProperties.Name="{loc:Str Key=Common_Close}"
                                        ToolTipService.ToolTip="{loc:Str Key=Common_Close}"
                                        Click="OnCloseRoomClick"
                                        DataContext="{x:Bind}">
                                        <FontIcon Glyph="&#xE711;" FontSize="11" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- 방 UI. DataContext = 선택된 ChatRoomViewModel -->
                    <StackPanel x:Name="RoomCard" Spacing="10" Visibility="Collapsed">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" Style="{ThemeResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
                                <Border Style="{StaticResource PillBorder}" Visibility="{Binding ThinkingVisibility}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <muxc:ProgressRing Width="12" Height="12" IsActive="True" />
                                        <TextBlock Text="{loc:Str Key=Room_Thinking}" FontSize="12" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <Button
                                Grid.Column="1"
                                Content="{Binding ToggleLabel}"
                                Command="{Binding ToggleViewCommand}"
                                AutomationProperties.AutomationId="RoomToggleButton" />
                        </Grid>

                        <!-- 터미널(원시)과 채팅(블록)은 같은 자리 -->
                        <Grid Height="460">
                            <terminal:TerminalHost
                                x:Name="Embedded"
                                Visibility="{Binding TerminalVisibility}" />

                            <Border
                                Style="{StaticResource CardBorder}"
                                Background="{ThemeResource LayerFillColorDefaultBrush}"
                                Visibility="{Binding ChatVisibility}">
                                <Grid>
                                    <Border
                                        Style="{StaticResource NoticeBorder}"
                                        VerticalAlignment="Top"
                                        Visibility="{Binding EmptyVisibility}">
                                        <TextBlock Text="{loc:Str Key=Room_Empty}" Style="{StaticResource PageSubtitleText}" />
                                    </Border>

                                    <ScrollViewer x:Name="ChatScroll" VerticalScrollBarVisibility="Auto">
                                        <ItemsRepeater
                                            AutomationProperties.AutomationId="ChatBlocks"
                                            ItemsSource="{Binding Blocks}">
                                            <ItemsRepeater.Layout>
                                                <StackLayout Spacing="2" />
                                            </ItemsRepeater.Layout>
                                            <ItemsRepeater.ItemTemplate>
                                                <DataTemplate x:DataType="vm:ChatBlockViewModel">
                                                    <Grid Padding="6,4" ColumnSpacing="10" Background="Transparent">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="36" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>

                                                        <!-- 왼쪽: 아바타(어시스턴트 첫 블록) 또는 사용자 강조 세로줄 -->
                                                        <Border
                                                            Grid.Column="0"
                                                            Width="28"
                                                            Height="28"
                                                            CornerRadius="14"
                                                            VerticalAlignment="Top"
                                                            Background="{x:Bind AvatarBrush}"
                                                            Visibility="{x:Bind AvatarVisibility}">
                                                            <TextBlock
                                                                Text="{x:Bind Avatar}"
                                                                Foreground="White"
                                                                FontSize="12"
                                                                FontWeight="SemiBold"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center" />
                                                        </Border>
                                                        <Rectangle
                                                            Grid.Column="0"
                                                            Width="3"
                                                            RadiusX="1.5"
                                                            RadiusY="1.5"
                                                            HorizontalAlignment="Left"
                                                            Margin="12,2,0,2"
                                                            VerticalAlignment="Stretch"
                                                            Fill="{ThemeResource AccentFillColorDefaultBrush}"
                                                            Visibility="{x:Bind UserBarVisibility}" />

                                                        <StackPanel Grid.Column="1" Spacing="2">
                                                            <Grid Visibility="{x:Bind HeaderVisibility}" ColumnSpacing="8">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{x:Bind SpeakerText}" Style="{ThemeResource BodyStrongTextBlockStyle}" FontSize="13" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Text="{x:Bind TimeText}"
                                                                    Style="{StaticResource MutedText}"
                                                                    VerticalAlignment="Center"
                                                                    ToolTipService.ToolTip="{x:Bind FullTimeText}" />
                                                            </Grid>

                                                            <!-- 보통 말(사용자·어시스턴트) -->
                                                            <StackPanel Spacing="2" Visibility="{x:Bind PlainVisibility}">
                                                                <TextBlock
                                                                    Text="{x:Bind DisplayText, Mode=OneWay}"
                                                                    Style="{ThemeResource BodyTextBlockStyle}"
                                                                    TextWrapping="Wrap"
                                                                    IsTextSelectionEnabled="True" />
                                                                <HyperlinkButton
                                                                    Content="{x:Bind MoreText, Mode=OneWay}"
                                                                    Visibility="{x:Bind MoreVisibility}"
                                                                    Command="{x:Bind ToggleExpandedCommand}"
                                                                    Padding="0"
                                                                    FontSize="12" />
                                                            </StackPanel>

                                                            <!-- 도구 호출·시스템: 접힌 회색 블록 -->
                                                            <muxc:Expander
                                                                HorizontalAlignment="Stretch"
                                                                Visibility="{x:Bind CollapsibleVisibility}">
                                                                <muxc:Expander.Header>
                                                                    <TextBlock Style="{StaticResource MutedText}">
                                                                        <Run Text="{x:Bind RoleText}" />
                                                                        <Run Text="·" />
                                                                        <Run Text="{x:Bind TimeText}" />
                                                                    </TextBlock>
                                                                </muxc:Expander.Header>
                                                                <TextBlock
                                                                    Text="{x:Bind DisplayText, Mode=OneWay}"
                                                                    Style="{StaticResource MonoText}"
                                                                    TextWrapping="Wrap"
                                                                    IsTextSelectionEnabled="True" />
                                                            </muxc:Expander>
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsRepeater.ItemTemplate>
                                        </ItemsRepeater>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- 입력 칸: Enter 보냄, Shift+Enter 줄바꿈 -->
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                x:Name="RoomInput"
                                Grid.Column="0"
                                AcceptsReturn="False"
                                TextWrapping="Wrap"
                                AutomationProperties.AutomationId="RoomInputBox"
                                PlaceholderText="{loc:Str Key=Room_InputPlaceholder}"
                                Text="{Binding Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                KeyDown="OnRoomInputKeyDown" />
                            <Button
                                Grid.Column="1"
                                Style="{ThemeResource AccentButtonStyle}"
                                Content="{loc:Str Key=Room_Send}"
                                AutomationProperties.AutomationId="RoomSendButton"
                                Command="{Binding SendCommand}" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
