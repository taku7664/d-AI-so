# 변경 기록

판 번호는 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

## 0.2.0 (2026-09-12)

앱 이름을 **DAIso** 하나로 통일하고, 창을 닫아도 맡긴 일이 계속 돌게 바꾼 판입니다.

### 이름

- 실행 파일이 `d-AI-so.exe` 에서 **`DAIso.exe`** 로 바뀌었습니다. 설치 폴더와 알림·작업 관리자에 뜨는 이름도 DAIso 입니다
- 자료 폴더가 `%LOCALAPPDATA%\DAIso` 로 옮겨 갑니다. **앱이 처음 뜰 때 쓰던 것을 통째로 옮깁니다** — 세션 인덱스·설정·보관해 둔 계정·프롬프트가 그대로 따라옵니다

### 기능

- **창을 닫아도 앱이 남습니다.** 알림 영역(작업 표시줄 오른쪽 끝)으로 들어가고, 맡겨 둔 도구는 계속 돕니다. 아이콘을 누르면 창이 돌아오고, 오른쪽 단추 메뉴에 열기·끝내기가 있습니다
- **일이 끝나면 윈도우 알림이 뜹니다.** 누르면 그 터미널 방으로 바로 갑니다. 한 번 끝날 때 한 번만 뜹니다
- **앱은 하나만 뜹니다.** 바로 가기를 다시 누르면 두 번째가 뜨는 대신 숨어 있던 창이 돌아옵니다
- **일이 끝난 방에 초록 점**이 켜집니다. 다른 화면을 보고 있거나 다른 앱을 쓰는 동안 끝난 일도 이제 표시됩니다 (예전에는 터미널 화면 안에서 탭을 옮길 때만 켜졌습니다)
- **로그인돼 있어도 다른 계정으로 로그인**할 수 있습니다. 로그인 단추가 항상 보입니다
- 계정 보관 이름을 팝오버 안에서 바로 적습니다. 저장 결과도 같은 자리에서 보입니다

### 고친 것

- **내 규칙 화면에 갔다 요약으로 돌아오면 터지던 문제.** 떠난 화면이 목록에 계속 붙어 있다가 죽은 컨트롤을 불렀습니다
- **계정 저장·삭제 확인 창이 뜨지 않고 조용히 지나가던 문제.** 창이 뜨기 전에 받아 둔 값이 비어 있었습니다
- **설정한 인덱스 경로를 쓸 수 없으면 앱이 창도 못 띄우고 죽던 문제.** 이제 기본 경로로 물러서고 이유를 설정 화면에 적습니다. 저장할 때도 미리 막습니다
- **다크 모드인데 상태 줄 글씨가 검게 나오던 문제**
- 창을 닫은 뒤 창 없는 프로세스가 남던 문제
- 터미널이 도구 이름을 보고 첫 프롬프트 깃발을 고르던 것을 도구가 정하게 바꿨습니다. 플러그인 도구도 로그인 인자와 첫 프롬프트 깃발을 정할 수 있습니다

## 0.1.1 (2026-09-11)

새 PC 에 깔아 보고 나온 것들입니다. 0.1.0 은 .NET 런타임이 따로 필요했고, Claude Code 를 새로 깐 PC 에서는 터미널이 열리지 않았습니다.

### 설치

- 설치 프로그램 하나로 끝납니다. .NET 런타임은 앱에 담겨 있고, WebView2 런타임은 없는 PC 에서만 설치 중에 깝니다. 규칙은 [docs/RELEASE.md](docs/RELEASE.md)
- Claude Code·Codex 를 npm 으로 깔려면 Node.js 가 필요합니다. 없으면 앱이 설치 버튼에서 내려받기 페이지를 열어 줍니다

### 고친 것

- Windows 기본 실행 정책(Restricted)인 새 PC 에서 터미널이 `claude.ps1 파일을 로드할 수 없습니다` 로 죽던 문제. PowerShell 에 `.cmd` 를 직접 넘겨 정책과 무관하게 뜹니다. 설치 버튼의 `npm` 도 같은 처리를 합니다

## 0.1.0 (2026-09-11)

첫 배포판입니다. 화면 일곱 개가 다 돌고, 도구를 YAML로 추가할 수 있는 상태입니다.

### 기능

- **도구 세 개 지원** — Claude Code, Codex CLI, Antigravity CLI. 계정·요금제·만료일을 카드로 보여줍니다
- **터미널** — 도구와 폴더를 고르면 앱 안에서 CLI가 뜹니다(ConPTY + xterm.js). 새 창으로 분리 가능
- **세션** — 프로젝트별 대화 기록, 본문 검색(FTS5 trigram), 이어서 열기, markdown 내보내기, 휴지통 삭제
- **사용량** — 일별·주별·월별 토큰 추이, 프로젝트별·모델별 집계, 단가표 기반 비용 추정
- **내 규칙** — `.daiso` 조건 트리 편집기와 마크다운 미리보기, `CLAUDE.md`·`AGENTS.md` 연동, 기본 제공 프리셋 16개
- **내 프롬프트** — 절차형 프롬프트 12개 기본 제공, 프로젝트에 넣고 첫 메시지로 보내기
- **계정 여러 개** — 로그인 상태를 이름 붙여 보관하고 되돌리기(DPAPI 암호화)
- **도구 직접 추가** — `%USERPROFILE%\.daiso\tools\*.yaml` 한 장으로 새 도구 등록. 대화 기록까지 읽히려면 stdio JSON 어댑터 연결

### 이번에 고친 것

배포 전 전면 검토에서 나온 것들입니다. 자세한 근거는 [docs/REVIEW_BACKLOG.md](docs/REVIEW_BACKLOG.md)에 있습니다.

- 인덱스가 판 올릴 때 빈 페이지를 남겨 **666MB까지 부풀던 문제**. 표를 버리고 다시 만든 뒤 VACUUM 하도록 바꿔 95MB로 줄었습니다
- 검색이 상한 없이 본문 전체를 메모리에 올리던 문제. 200건 상한과 배경 스레드 실행
- 같은 대화 줄이 중복 저장되던 문제(실측 1,953행). 저장소에서 한 번 더 막습니다
- 도구 호출 기록이 대화로 분류돼 검색을 덮던 문제(실측 11,424행)
- 첫 메시지에 든 `$(…)`가 PowerShell에서 실행되던 문제
- 플러그인을 지운 뒤 그 세션을 열면 앱이 죽던 문제
- 화면을 옮겨다닐 때 처리기가 쌓여 같은 작업을 여러 번 하던 문제
- 어댑터가 stderr에 많이 쓰면 멈추던 문제
- 방을 닫아도 손자 프로세스가 남던 문제. 작업 개체로 묶었습니다

### 아는 제약

- Windows 10 1809 이상, x64만 지원합니다
- .NET 8 Desktop Runtime이 필요합니다(Windows App SDK는 앱에 포함돼 있습니다)
- 화면 문구는 한국어뿐입니다
- 코드 서명이 없어서 처음 실행할 때 SmartScreen 경고가 뜹니다
- 배포 zip은 `dotnet publish` 산출물이 아니라 Release 빌드 폴더입니다.
  WinUI unpackaged 앱을 publish 하면 컴파일된 XAML(`.xbf`)이 빠져서 실행되지 않습니다
