# 변경 기록

판 번호는 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

## 0.1.0 (2026-09-11)

첫 배포판입니다. 화면 일곱 개가 다 돌고, 도구를 YAML로 추가할 수 있는 상태입니다.

### 기능

- **도구 세 개 지원** — Claude Code, Codex CLI, Antigravity CLI. 계정·요금제·만료일을 카드로 보여줍니다
- **터미널** — 도구와 폴더를 고르면 앱 안에서 CLI가 뜹니다(ConPTY + xterm.js). 새 창으로 분리 가능
- **세션** — 프로젝트별 대화 기록, 본문 검색(FTS5 trigram), 이어서 열기, markdown 내보내기, 휴지통 삭제
- **사용량** — 일별·주별·월별 토큰 추이, 프로젝트별·모델별 집계, 단가표 기반 비용 추정
- **내 규칙** — `.daiso` 조건 트리 편집기와 마크다운 미리보기, `CLAUDE.md`·`AGENTS.md` 연동, 기본 제공 프리셋 16개
- **내 프롬프트** — 절차형 프롬프트 12개 기본 제공, 프로젝트에 넣고 첫 메시지로 보내기
- **계정 여러 개** — 로그인 상태를 이름 붙여 보관하고 되돌리기(DPAPI 암호화)
- **도구 직접 추가** — `%USERPROFILE%\.daiso\tools\*.yaml` 한 장으로 새 도구 등록. 대화 기록까지 읽히려면 stdio JSON 어댑터 연결

### 이번에 고친 것

배포 전 전면 검토에서 나온 것들입니다. 자세한 근거는 [docs/REVIEW_BACKLOG.md](docs/REVIEW_BACKLOG.md)에 있습니다.

- 인덱스가 판 올릴 때 빈 페이지를 남겨 **666MB까지 부풀던 문제**. 표를 버리고 다시 만든 뒤 VACUUM 하도록 바꿔 95MB로 줄었습니다
- 검색이 상한 없이 본문 전체를 메모리에 올리던 문제. 200건 상한과 배경 스레드 실행
- 같은 대화 줄이 중복 저장되던 문제(실측 1,953행). 저장소에서 한 번 더 막습니다
- 도구 호출 기록이 대화로 분류돼 검색을 덮던 문제(실측 11,424행)
- 첫 메시지에 든 `$(…)`가 PowerShell에서 실행되던 문제
- 플러그인을 지운 뒤 그 세션을 열면 앱이 죽던 문제
- 화면을 옮겨다닐 때 처리기가 쌓여 같은 작업을 여러 번 하던 문제
- 어댑터가 stderr에 많이 쓰면 멈추던 문제
- 방을 닫아도 손자 프로세스가 남던 문제. 작업 개체로 묶었습니다

### 아는 제약

- Windows 10 1809 이상, x64만 지원합니다
- .NET 8 Desktop Runtime이 필요합니다(Windows App SDK는 앱에 포함돼 있습니다)
- 화면 문구는 한국어뿐입니다
- 코드 서명이 없어서 처음 실행할 때 SmartScreen 경고가 뜹니다
- 배포 zip은 `dotnet publish` 산출물이 아니라 Release 빌드 폴더입니다.
  WinUI unpackaged 앱을 publish 하면 컴파일된 XAML(`.xbf`)이 빠져서 실행되지 않습니다
