# 코드 검토 잔여 건

> [UI_TOKENS_PLAN.md](UI_TOKENS_PLAN.md) 가 끝난 뒤 남은 것들.
> 2026-09-10 전면 검토에서 나온 지적 중 아직 손대지 않은 것을 여기서 0으로 만든다.
> 각 건은 **커밋 하나 + 이 문서 갱신**이다.

## 원칙

- **고친다**: 동작이 틀렸거나, 조용히 실패하거나, 값이 여러 곳에 흩어진 것.
- **보고만 한다**: 화면이 실제로 달라지거나, 앱이 하는 일의 범위가 바뀌는 것.
  이건 버그가 아니라 다음 리팩터링 건이다.
- 앱은 실행하지 않는다. 확인은 빌드 · 테스트 · 기계 검증으로 한다.

---

## 작업 목록

| # | 건 | 성격 | 상태 |
|---|---|---|---|
| R1 | `ToolLook` 이 모든 `ToolKind` 를 다루는지 검사하는 테스트 | 조용한 실패 | ✅ |
| R2 | 종료 시 `ServiceProvider` 를 해제하지 않아 SQLite 연결이 안 닫힘 | 자원 누수 | ✅ |
| R3 | 설정 저장 실패를 아무도 모름 | 조용한 실패 | ✅ |
| R4 | `SessionsViewModel` 의 `IsBusy` 가드가 다시 읽기를 삼킴 | 동작 오류 | ✅ |
| R5 | 페이지를 추가할 때 배선을 빠뜨리면 조용히 요약 화면이 뜸 | 조용한 실패 | ✅ |
| R6 | 페이지 트리 깊이 | 구조 | ✅ (진단이 틀렸었다 — 아래) |

## 보고만 하는 것 (작업하지 않음)

| 건 | 왜 안 하는가 |
|---|---|
| 글자 10단계 → 5단계 | 화면 글자 크기가 실제로 달라진다 |
| 간격 `10` · `14` 를 4배수로 스냅 (33곳) | 화면 밀도가 달라진다 |
| `Width`/`Height` 276곳 · `Padding`/`Margin` 59곳 토큰화 | 자리마다 뜻이 달라 묶으면 거짓말이 된다 |
| 마이그레이션을 도구 3개로 확장 | `MigrationDirection` 이 2개 전제다. 새 기능이지 버그가 아니다 |
| Provider 계층의 한글 화면 문구 (`AuthStatus.Extras`) | `IProvider` 계약을 바꾸는 일이다. ARCHITECTURE 정본 변경 건 |
| `DataTemplate` 29개를 컴포넌트로 빼기 | `{x:Bind}` 컴파일 바인딩을 잃는다. 그게 터미널·내 규칙 코드비하인드가 845·770줄이 된 원인이다 |
| 화면 골격(`Pivot` · `ScrollViewer`) 재설계 | 화면 구성 자체를 바꾸는 일이다 |

---

---

## 2차 — 실제 실행에서 나온 버그 + 승인된 리팩터

2026-09-10 앱을 띄워 보고 나온 것과, 앞서 "보고만" 으로 미뤄 뒀다가 승인된 것들.

| # | 건 | 성격 | 상태 |
|---|---|---|---|
| B1 | 제목줄 뒤로 단추만 크기·글리프가 다름 | 화면 | ✅ |
| B2 | 도구 탭을 빨리 바꾸면 목록이 앞 탭에 머묾 | 동작 오류 | ✅ |
| B3 | 저장 실패 같은 알림을 좌측 하단에 빨갛게 모으기 | 화면 | ✅ |
| A1 | 글자 10단계 → 5단계, 간격 `10`·`14` 눈금 스냅 | 디자인 | ✅ |
| A2 | `Width`/`Height` 토큰화 | 디자인 | ✅ (재 보니 대부분은 묶으면 안 되는 것이었다) |
| A3 | `DataTemplate` 컴포넌트화 | 구조 | ✅ (사전으로. 컴포넌트가 아니라) | 
| A4 | 화면 골격 재설계 | 구조 | ✅ (재 보니 골격은 이미 최소였다) |
| A5 | 마이그레이션을 도구 3개로 확장 | 기능 | ✅ |
| A6 | Provider 계층 한글 문구 → resw | 계약 | ✅ |
| T1 | 터미널 방 도구 줄이 창 하한에서 넘침 | 화면 | ✅ |

### 원인 (고치기 전에 찾은 것)

- **B1** — 뒤로는 `muxc:TitleBar` 의 **내장** 단추이고, 앞으로·메뉴는 `LeftHeader` 에 직접 그린
  `36x32` 단추(글리프 16)다. 내장 단추의 글리프 크기는 WinUI 가 정하므로 둘이 맞을 수가 없다.
  상자 크기를 맞춰 둔 주석은 있었지만 **글리프는 맞출 방법이 없었다.**
  → 뒤로도 `LeftHeader` 로 내려 셋을 같은 단추로 만든다.
- **B2** — `ReloadForFilter()` 가 `if (!IsBusy)` 로 감싸여 있어, **읽는 중에 바뀐 필터를 통째로 버린다.**
  R4 에서 고친 것과 같은 결함이 한 곳 더 있었던 것이다.
  Codex 를 누른 직후(아직 읽는 중) 전체를 누르면 탭만 전체가 되고 목록은 Codex 인 채로 남는다.
  - *정정:* R4 기록에 "`ApplyFiltersCommand` 는 어디서도 바인딩되지 않는다"고 적었는데 **틀렸다.**
    XAML 에서 안 쓸 뿐 `ReloadForFilter()` 가 쓰고 있었다. 지웠으면 필터가 통째로 멎을 뻔했다.
- **B3** — R3 에서 저장 실패를 설정 화면 안에만 띄웠다. 설정 화면을 안 보고 있으면 못 본다.
  셸 하단 상태바(`StatusBar`)가 이미 있으니 거기로 모은다. 알림 통로를 하나로 만들어 두면
  앞으로 다른 실패도 같은 자리에 붙일 수 있다.

## 작업 기록

- **R1** — `ToolLookTests` 추가. `Title` · `Vendor` · `Short` · `Initial` · `Color` · `LogoPath` 여섯 switch 와
  `DisplayOrder` 가 모든 `ToolKind` 를 다루는지 본다.
  `ToolKind` 에 `Cursor` 를 임시로 더해 보니 **8곳이 정확히 빨개졌다**. 원복 확인.
  - 폴백(`_ =>`)을 지울 수는 없다 — switch 식은 모든 입력을 받아야 한다. 그래서 "빠진 것"을 밖에서 본다.
- **R2** — `App.DisposeServices()` 를 만들고 `ShellWindow.OnClosed` 가 **설정을 쓴 다음에** 부른다.
  싱글턴 중 `IDisposable` 인 것들(특히 `SqliteSessionIndex` 의 쓰기 연결)이 닫히고 WAL 체크포인트가 돈다.
  해제 자체가 실패해도 종료를 막지 않는다.
- **R3** — 처음에 "실패하면 `Changed` 를 쏘지 말자"로 고쳤다가 **되물렀다**.
  메모리의 `Current` 는 이미 바뀐 뒤라, 알림을 막으면 화면이 메모리 값과도 어긋난다.
  진짜 결함은 재계산이 아니라 **실패를 아무도 모른다**는 것이었다.
  `ISettingsStore.LastSaveError` 를 두고, 설정 화면의 파일 위치 아래에 까닭을 붉게 한 줄 띄운다.
  까닭은 `SensitiveTextMasker` 를 거쳐 적는다 (예외 메시지에 경로·토큰이 섞일 수 있다).
- **R4** — 세대 번호로 바꿨다. 여기는 요약·사용량 화면과 사정이 다르다:
  `LoadCommand` 와 삭제 뒤의 `await LoadAsync(default)` 는 **서로를 끊지 않는다**(뒤엣것은 취소 토큰이 없다).
  그래서 세대 번호를 `await` **뒤에도** 확인해, 늦게 온 옛 결과가 화면을 덮지 않게 했다.
  요약·사용량 쪽은 두 실행이 같은 명령이라 앞의 토큰이 끊기므로 이 확인이 따로 필요 없다.
  - *덧:* `ApplyFiltersCommand` 는 XAML·코드 어디서도 바인딩되지 않는다. 남은 껍데기지만 해롭지 않아 두었다.
- **R5** — `ShellNavigationTests` 추가. 넷을 잠근다:
  메뉴 `Tag` 마다 `Navigate` switch 에 자리가 있는가 · switch 가 가리키는 화면 파일이 있는가 ·
  **메뉴에서 갈 수 없는 화면 파일이 있는가**(`PageSkeletonTests` 는 개수만 세고 도달 가능성은 안 본다) · 메뉴가 일곱인가.
  메뉴 항목만 더하고 배선을 빼 보니 **둘이 정확히 빨개졌다**. 원복 확인.
  - XAML 을 글이 아니라 `XDocument` 트리로 읽는다. 속성 순서나 줄바꿈이 바뀌어도 흔들리지 않는다.
- **R6** — **먼저 재고 나서 계획이 틀렸다는 걸 알았다.**
  제가 앞서 "`Card`·`StatTile` 어휘를 만들면 깊이 16 → 4"라고 했는데, 실제로 재 보니 그렇지 않다.
  `SessionsPage` 의 가장 깊은 경로는 이렇다:

  ```
  Page > PageBody > Grid > Border > Pivot > PivotItem > Grid > ScrollViewer
       > StackPanel > ItemsRepeater > DataTemplate > Grid > StackPanel > StackPanel > Border
  ```

  - 앞의 **8겹은 화면 골격**이다. `Card` 같은 컴포넌트로는 한 겹도 안 준다.
  - 뒤의 겹은 **`DataTemplate` 안**이다. 여기를 컴포넌트로 빼면 `{x:Bind}` 컴파일 바인딩을 잃고
    `PropertyChanged` 를 손으로 구독하게 된다 — 터미널·내 규칙 화면의 코드비하인드가 845·770줄이 된 바로 그 길이다.
  - `Card` 가 없앨 수 있는 것은 `Border`+`StackPanel` 두 겹뿐이고, 그마저 깊이가 문제가 아닌 화면들에서만 그렇다.

  그래서 어휘를 짓는 대신 **잰 것에서 나온 것만** 했다:

  1. `SessionsPage` 456줄의 **껍데기 `StackPanel`** 을 없앴다. 자식이 둘인데 둘은 서로 배타적 가시성이라
     같은 격자 칸에 놓으면 된다. 들여쓰기도 어긋나 있던 걸로 보아 편집하다 남은 것이다. 16 → 15겹(새 기준 16).
  2. **깊이 래칫 테스트** `PageDepthTests` 를 켰다. 지금 값을 적어 두고 더 깊어지면 실패한다.
     예산보다 2겹 이상 얕아져도 실패한다 — 줄였으면 표를 내려 래칫을 조이라는 뜻이다.
     새 화면을 예산에 안 적어도 실패한다.
     `UsagePage` 에 `StackPanel` 하나를 덧대 보니 정확히 빨개졌다. 원복 확인.
  - *도중에 잡은 것:* 첫 검사기는 `<TextBlock />` 같은 자기 닫는 잎을 겹으로 안 셌다.
    그러면 같은 트리도 `<X/>` 로 쓰느냐 `<X></X>` 로 쓰느냐에 따라 값이 달라진다.
    형식이 아니라 트리를 재도록 고쳤다(그래서 예산 수가 1씩 올라갔다).
- **B1** — 뒤로를 `LeftHeader` 로 내려 앞으로·메뉴와 같은 `36x32` 단추(글리프 16, `E72B`)로 만들었다.
  `TitleBar.IsBackButtonVisible=False`. 내장 단추가 사라졌으니 `TitleBarBackButtonWidth` 자원과
  가드 테스트의 예외 항목도 같이 지웠다.
- **B2** — `ReloadForFilter` 의 `if (!IsBusy)` 를 걷어냈다. 동시성은 `LoadAsync` 의 세대 번호가 이미 막는다.
  `ResetFilters` 는 다섯 값을 한꺼번에 바꾸므로 읽기를 다섯 번 시작하지 않도록 `_suspendReload` 로 묶고 끝에 한 번만 읽는다.
- **B3** — `ShellViewModel.Report(string?)` 통로 하나를 만들고, 좌측 하단 상태바가 알림을 빨갛게
  (`SystemFillColorCriticalBrush`) 그린다. 알림이 있으면 진행 문구보다 먼저 보인다 — 실패가 더 급하다.
  설정 저장 실패는 `ISettingsStore.Changed` 를 타고 올라오고, 다음 저장이 성공하면 저절로 지워진다.
  R3 에서 설정 화면 안에 넣었던 줄과 `SaveFailed`/`SaveFailureText` 속성은 걷어냈다 — 알림은 한 자리에만 뜬다.
- **A1** — 토큰 34개 → **24개**. 58곳을 옮겼다. 화면이 실제로 달라지는 변경이다.
  - 글자: `12 · 14 · 16 · 20 · 28` 다섯 단계. 접은 내역 `11→12` `13→14` `18→16` `22→20` `24→20`.
    **다섯 다 이미 쓰이던 값**이라 낯선 크기가 새로 생기지 않았다. 가장 많이 바뀐 건 13→14 (18곳: 부제·표의 숫자).
  - 간격: `10→8`(27곳) `14→12`(6곳). 규칙은 *가장 가까운 4의 배수, 동점이면 내림*.
    올림이 아니라 내림인 이유 — 33곳이 한꺼번에 벌어지면 창 하한(1024x700)에서 내용이 아래로 밀린다.
  - `1 · 2 · 3 · 6` 은 그대로 뒀다. 아이콘·배지 안쪽 간격이라 화면 눈금과 성격이 다르다.
- **A6** — `AuthStatus.Extras` 의 타입을 `IReadOnlyList<string>` → `IReadOnlyList<AuthNote>` 로 바꿨다.

  ```csharp
  public sealed record AuthNote(string Key, string? Argument = null);
  ```

  Provider 는 이제 **어느 나라 말도 모른다.** 문구 키와 값만 내놓고, 그리는 쪽(앱)이 resw 에서 찾는다.
  `Daiso.Providers.*` 의 한글 문자열은 **0개**가 됐다(남은 것은 `·` `…` 같은 구분 기호뿐).
  문구 17개를 resw 로 옮겼다.
  - 화면에 보이는 글자는 그대로다 — `subscription: max` 같은 줄도 `subscription: {0}` 문구로 옮겨,
    이제는 그 라벨까지 번역·수정할 수 있다.
  - **테스트에서도 한글 문장이 사라졌다.** 예전에는 `joined.Should().Contain("만료: 알 수 없음")` 처럼
    Provider 테스트가 한국어 문장을 검사하고 있었다. 지금은 키를 검사한다.
  - *도중에 잡은 것:* `StringResourceKeysTests` 가 `src/Daiso.App` 만 훑고 있어서 새 키 17개를
    전부 "죽은 키"로 잡았다. **테스트가 정확히 일한 것이다** — resw 를 부르는 곳이 App 밖으로 늘어난 사실을
    알려 준 것이라, 스캔 범위를 `src/` 전체로 넓혔다.
  - CLI(`Daiso.Cli`)는 resw 를 읽지 않으므로 `키 = 값` 을 그대로 찍는다. 진단용 도구라 그편이 낫다.
- **A5** — 마이그레이션을 도구 수와 무관하게 만들었다.

  | | 전 | 후 |
  |---|---|---|
  | 방향 | `enum { ClaudeToCodex, CodexToClaude }` | `record struct MigrationDirection(ToolKind From, ToolKind To)` |
  | 계획 | `Plan(claude, codex)` | `Plan(tools, sources, left, right)` |
  | 고를 수 있는 방향 | 2 (고정) | **6** (도구 3개 기준, `plan.Directions` 가 만든다) |
  | 화면 | 버튼 두 개 | 방향 목록에서 하나 고르고 `옮기기` |

  - **import 전개 규칙을 능력으로 바꿨다.** 예전에는 "대상이 Codex 면 전개"라고 도구 이름으로 박혀 있었다.
    지금은 `IProvider.SupportsInstructionImports` 를 본다. **기본값은 false** —
    확인되지 않은 도구로 옮길 때는 내용을 펼쳐 두므로 안전한 쪽으로 틀린다.
    Claude 만 true 로 확인해 두었다. Antigravity(`GEMINI.md`)의 import 지원 여부는 확인하지 못해 false 다.
  - Core 의 안내·경고도 완성 문장을 버리고 `MigrationNote(Key, Argument, Argument2)` 가 됐다 (A6 와 같은 원칙).
    문구 9개를 resw 로 옮겼다.
  - diff 는 본디 둘 사이의 것이라 비교 짝은 밖에서 고른다. 기본은 **파일이 실제로 있는 도구 둘**이다.
  - CLI 는 `--to` 에 더해 `--from` 을 받는다. 도구가 셋이면 반대편이 하나로 정해지지 않기 때문이다.
  - *도중에 잡은 것:* `Migration_ToCodex` · `Migration_ToClaude` 문구 두 개가 죽은 키로 남았다.
    방향 두 개 전용 버튼 이름이었다. **`StringResourceKeysTests` 가 잡아 줬다.**
- **A2** — **먼저 재고 나서 범위가 줄었다.**
  - *정정:* 제가 앞서 말한 "276곳"은 과대치였다. `obj/` 의 빌드 사본과 복합 속성명(`MinWidth` 를 `Width` 로 중복)
    까지 세고 있었다. 실제는 **107곳 · 54종**이다.
  - 그중 **두 파일 이상에서 쓰이는 값은 7종 37곳**뿐이었다. 나머지 47종 70곳은 한 파일 전용이다.
  - 그 7종을 하나씩 열어 보니 **대부분이 우연이었다**:

    | 값 | 어디에 |
    |---|---|
    | `160` | 진행 막대 폭 · 표의 열 폭 · 타일 최소 폭 — **셋 다 다른 것** |
    | `220` | 텍스트 상자 최소 폭 · 콤보 최소 폭 — 다른 것 |
    | `120` | 필터 콤보 폭 · 단가표 열 폭 — 다른 것 |
    | `200` | 설정 입력 폭 · 찾기 상자 폭 · 카드 폭 — 다른 것 |
    | `10` | 상태 점(Ellipse) — **같은 것** ✔ |

    같은 숫자를 한 토큰으로 묶으면, 진행 막대를 넓히려다 표의 열이 딸려 온다.
  - 그래서 **뜻이 같은 것만** 올렸다: `StatusDotSize`(10, 8곳) · `FilterComboWidth`(120) ·
    `SettingInputWidth`(200, 4곳) · `TitleBarButtonWidth`/`Height`(36x32, 단추 3개).
  - 나머지는 그대로 두었다. 이건 미룬 것이 아니라 **재 보고 내린 결론**이다 —
    숫자가 같다는 것과 결정이 같다는 것은 다르다.
- **A3** — 이 건을 미뤘던 이유는 "컴포넌트로 빼면 `{x:Bind}` 를 잃는다"였다. **그건 코드 컨트롤로 뺄 때 이야기다.**
  `x:Class` 를 가진 `ResourceDictionary` 로 옮기면 컴파일 바인딩이 그대로 산다. 그래서 그 길로 갔다.
  - 27개 템플릿을 재 보니 **핸들러가 없고 페이지 멤버도 참조하지 않는 자기완결적인 것이 9개**였다
    (세션 8 · 프롬프트 1). 그것만 `src/Daiso.App/Ui/ItemTemplates.xaml` 로 옮겼다.
  - 결과:

    | | 전 | 후 |
    |---|---:|---:|
    | SessionsPage 깊이 | 16 | **11** |
    | SessionsPage 줄 수 | 659 | **470** |

  - 나머지 18개는 **옮기지 않았다.** 이벤트 핸들러 24개가 페이지 코드비하인드를 부르고 있어,
    옮기려면 그 핸들러가 사전 코드비하인드로 가서 페이지에 닿을 길을 새로 내야 한다.
    그건 기계적 이동이 아니라 결합을 바꾸는 일이라 **별개 건**이다.
  - **눈속임 방지:** 옮겨 간 사전(`ItemTemplates.xaml`, 깊이 7)도 깊이 예산에 함께 적었다.
    안 적으면 "옮겨서 얕아진 것"이 수치만 좋아 보인다.
  - 가드 `ItemTemplateTests` 4종: 화면이 부르는 키가 사전에 있는가(**없으면 빌드가 아니라 실행 중에 터진다**) ·
    아무도 안 쓰는 템플릿이 있는가 · 모든 템플릿에 `x:DataType` 이 있는가(없으면 `{x:Bind}` 가 조용히
    느린 `{Binding}` 으로 밀린다) · 사전에 핸들러가 섞여 들지 않았는가.
  - 앱을 띄워 확인했다 — 사전 병합이 실패하면 앱이 시작조차 못 한다. 시작됐다.
- **A4** — **재 보니 이 건의 대상이 A3 뒤에 사라져 있었다.**
  - 속성 없는 한 자식 껍데기 컨테이너를 전수 조사했더니 **0개**였다 (R6 에서 마지막 하나를 없앴다).
  - `SessionsPage` 의 골격은 `Page > PageBody > Grid > Border > Pivot > PivotItem > Grid` 7겹인데,
    `Grid`=2단 배치, `Border`=카드 테두리, `Pivot`=탭이다. **셋 다 뜻이 있다.** 뺄 것이 없다.
  - 남은 깊이의 진짜 원인은 골격이 아니라 **핸들러를 가진 목록 템플릿**이었다.
    그래서 A3 를 이어서, 옮길 수 있는 것 8개를 더 옮겼다 (내 규칙 2 · 터미널 4 · 사용량 2).

    | 화면 | 깊이 | 줄 수 |
    |---|---|---|
    | UsagePage | 10 → **9** | 275 → **205** |
    | TerminalPage | 14 | 879 → **815** |
    | RuleMakerPage | 14 | 612 → **589** |
    | SessionsPage | 16 → **11** | 659 → **470** |

  - **더 못 옮기는 이유를 정확히 적어 둔다.** 남은 10개 템플릿의 핸들러는
    `XamlRoot`(대화상자를 띄울 창) · `ShowAsync` · `ViewModel.LoadCommand` 를 쓴다. 즉 **정말로 페이지가 필요하다.**
    옮기려면 "대화상자를 누가 소유하는가"를 바꿔야 한다 — 예를 들어 대화상자 서비스를 두고
    템플릿은 뷰모델 명령만 부르게. 그건 앱을 짜는 방식을 바꾸는 일이라 **여기서 하지 않는다.**
    `DashboardPage` 가 16겹으로 남은 것이 정확히 그 이유다.
- **T1** — 터미널 방 도구 줄. 아이콘 여덟 개를 늘어놓고 방 이름에 고정 `MaxWidth="520"` 을 준 구조였다.
  재 보니 **창 하한에서 이미 넘치고 있었다**:

  | | 전 | 후 |
  |---|---:|---:|
  | 창 하한에서 쓸 수 있는 폭 | 766px | 766px |
  | 평소 줄 | 294 + 방 이름 520 = **814px** (넘침) | 190 + 8 = **198px** |
  | 찾기 열었을 때 | **1084px** (크게 넘침) | **336px** |
  | 방 이름 | 고정 520 | 남는 폭(`*` 열) — **넘칠 수 없음** |

  바뀐 것 셋:
  1. **방 이름을 `*` 열로.** 도구가 아니라 표시라 오른쪽에 두고 남는 폭만 쓴다.
  2. **찾기가 덧붙지 않고 줄을 대신한다.** 폭이 늘지 않는다. 닫기 단추도 줄 안에 넣었다.
  3. **`Ctrl+F` 를 붙였다.** 전에는 아이콘 클릭이 유일한 길이었다(`KeyboardAccelerator` 0개).
     WebView2 가 포커스를 잡으면 앱 단축키가 안 닿아, `xterm` 키 핸들러가 먹고
     `TerminalHost.FindRequested` 로 올려 준다 — `Ctrl+1~7`·`F5` 를 가로채던 자리와 같은 길이다.

  덜어낸 것: 늘 쓰지 않는 셋(**입력 비우기 · 같은 폴더로 새 터미널 · 규칙 편집**)을 더보기 안으로 넣었다.
  상시 노출은 넷이다 — 찾기 · 프롬프트 넣기 · 파일 첨부 · 폴더 열기. 넷 다 **다른 길이 없는 것**이다.
  - *덧:* 규칙 편집은 이 방에 하는 일이 아니라 다른 화면으로 나가는 것이라 메뉴에서도 구분선 아래에 뒀다.
  - *도중에 잡은 것:* 아이콘을 메뉴 항목으로 바꾸니 `Room_ClearInputTip`("Ctrl+E, Ctrl+U 를 보냅니다")이
    죽은 키가 됐다. 지우지 않고 메뉴 항목 툴팁으로 살렸다 — 이 단추가 **실제로 무엇을 보내는지**가 그 문구뿐이다.
